[package]
name = "dailycockpit"
version = "0.1.0"
description = "Project-centric local-first AI development cockpit"
authors = ["Alan"]
edition = "2021"

[lib]
name = "dailycockpit_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.6.3", features = [] }

[dependencies]
tauri = { version = "2.11.3", features = [] }
