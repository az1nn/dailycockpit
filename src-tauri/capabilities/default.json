{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the Daily Cockpit main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
